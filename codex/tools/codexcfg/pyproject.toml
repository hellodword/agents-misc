[project]
name = "codexcfg"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "packaging",
  "rich",
  "tomlkit",
]

[project.scripts]
codexcfg = "codexcfg.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
