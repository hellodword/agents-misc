# Agent Rules Evals

These JSONL cases are lightweight regression checks for the rules kit.

Use them when changing routing, priority, safety invariants, companion metadata, or final response expectations.

Each case states the task, optional touched paths, expected rules, forbidden rules, and expected behavior. The cases are examples for human or agent review; they are not tied to one runner.
