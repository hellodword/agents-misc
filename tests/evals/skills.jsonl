{"id":"skill-ai-visual-positive","task":"The user explicitly asks for screenshot visual QA of a web UI."}
{"id":"skill-ai-visual-negative","task":"A bug report happens to contain a screenshot."}
{"id":"skill-atomic-positive","task":"The user asks to commit the verified change."}
{"id":"skill-atomic-negative","task":"Tests passed, but nobody requested a commit."}
{"id":"skill-browser-positive","task":"Add and run a durable Playwright regression for browser navigation behavior using the project's existing Playwright configuration; the user does not request a display-mode override or browser reuse."}
{"id":"skill-browser-negative","task":"Run an existing Vitest and React Testing Library component test for a button whose behavior does not depend on browser navigation, layout, or native browser APIs."}
{"id":"skill-compat-positive","task":"Change an existing documented CLI exit code."}
{"id":"skill-compat-negative","task":"Rename an unexported local variable."}
{"id":"skill-converge-positive-audit","task":"Use $converge-codebase on this repository to find obsolete internal implementation and low-value tests."}
{"id":"skill-converge-positive-execute","task":"Use /converge-codebase to implement a subsystem-wide convergence of internal handwritten code and tests while preserving current behavior."}
{"id":"skill-converge-negative-broad-cleanup","task":"Aggressively clean up this whole repository, remove legacy code, prune low-value tests, and de-bloat it while preserving behavior."}
{"id":"skill-converge-negative-sqlite-migrations","task":"Collapse the old SQLite migration chain into the current schema baseline."}
{"id":"skill-converge-negative-generated-output","task":"Remove stale tracked generated parser output and rerun its generator."}
{"id":"skill-converge-explicit-specialist-boundary","task":"Use /converge-codebase to collapse existing SQLite migrations and rewrite tracked generated bindings."}
{"id":"skill-environment-positive","task":"A missing display blocks a required headful non-browser UI test."}
{"id":"skill-environment-negative","task":"The project environment is working and a source assertion fails."}
{"id":"skill-generated-positive","task":"Add tracked generated API bindings consumed by the build."}
{"id":"skill-generated-negative","task":"Edit an ordinary handwritten source file."}
{"id":"skill-nix-positive","task":"Restructure flake checks and the Just validation recipe."}
{"id":"skill-nix-negative","task":"Fix application copy in a repository that does not use Nix."}
{"id":"skill-patch-positive","task":"Maintain patches in an existing Nix-backed patch repository against an immutable upstream revision."}
{"id":"skill-patch-negative","task":"Modify source directly in a normal fork."}
{"id":"skill-sqlite-positive","task":"Change the existing SQLite migration ledger and backup flow."}
{"id":"skill-sqlite-negative","task":"The product mentions a database but has not selected SQLite."}
{"id":"skill-plan-positive","task":"Write a standalone unattended execution plan another agent can copy."}
{"id":"skill-plan-negative","task":"Implement a complex task and provide a routine final report."}
